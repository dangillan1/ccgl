# Archive reports (generated hourly, don't need in repo)
reports/*.html

# Setup script (one-time use)
setup-repo.sh

# macOS
.DS_Store

# Python
__pycache__/
*.pyc

# Backups (pre-redesign snapshots, etc.)
backups/

# Temp debug data files
data/_*.json

# Editor
*.swp
*.swo
*~

# Secrets (GrowLink API bearer token, captured from browser localStorage)
data/config.json

# Logs (may contain credentials in error output)
logs/
